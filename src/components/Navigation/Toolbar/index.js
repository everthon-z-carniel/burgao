import React from 'react';
import Toolbar from './styles';

const toolbar = (props) => (
  <Toolbar>
    <div>MENU</div>
    <div>Logo</div>
    <nav style={{height: "80%"}}>
      ...
    </nav>
  </Toolbar>
);

export default toolbar;